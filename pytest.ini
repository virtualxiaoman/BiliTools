[pytest]
# 测试发现：tests/ 目录下的 test_*.py
testpaths = tests
# 统一编码，避免 Windows 控制台中文乱码影响断言
python_files = test_*.py

# 自定义标记：真实网络测试（默认跳过，--network 或 BILITOOLS_NETWORK=1 启用）
markers =
    network: 真实网络测试，需要能访问 B 站且 cookie 已登录
