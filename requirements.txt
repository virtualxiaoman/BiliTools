# BiliTools dependencies (UTF-8)
# Direct dependencies, versions confirmed in venv
certifi==2026.1.4
charset-normalizer==3.4.4
et-xmlfile==2.0.0
idna==3.11
numpy==2.4.2
openpyxl==3.1.5
pandas==3.0.0
pillow==12.1.0
python-dateutil==2.9.0.post0
qrcode==8.2
requests==2.32.5
six==1.17.0
tzdata==2025.3
urllib3==2.6.3
imageio-ffmpeg==0.6.0            # wheel 内置静态 ffmpeg 二进制：无系统 ffmpeg 时用于音视频合成回退
PySide6==6.11.1            # 桌面前端 GUI（python -m frontend.pyside6）

# Runtime note:
#   Audio/video merging uses ffmpeg. Priority: system ffmpeg (invoked via
#   subprocess) → the static ffmpeg bundled by imageio-ffmpeg (installed above).
#   So ffmpeg no longer needs to be installed manually.
#   Reference: BV1qw4m1d7hx

# Packaging note (optional, only needed to build the desktop exe):
#   pip install pyinstaller==6.22.0
#   pyinstaller bilitools.spec --noconfirm   # 产物在 dist/BiliTools.exe
